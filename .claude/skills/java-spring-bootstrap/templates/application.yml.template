spring:
  application:
    name: ${APP_NAME:{{artifactId}}}
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:development}

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
