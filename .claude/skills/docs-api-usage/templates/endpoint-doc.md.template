# <Feature> - API Usage

**Base URL:** `/api/v1`  
**Auth:** `<PUBLICA | USER | ADMIN | ROLE(S)>`  
**Content-Type:** `application/json`

## Objetivo

Breve descricao do que a funcionalidade resolve e quem pode usa-la.

## Endpoints

### <METHOD> <PATH>
**Descricao:**  
**Idempotencia:** `<Sim | Nao>`  
**Rate Limit:** `<opcional>`  
**Headers:**
- `Authorization: Bearer <token>` (se aplicavel)

**Parametros de query:** (se aplicavel)
- `<param>`: `<descricao>` (default: `<valor>`)

**Path params:** (se aplicavel)
- `<param>`: `<descricao>`

**Requisicao:**
```json
{
  "example": "value"
}
```

**Resposta <STATUS>:**
```json
{
  "example": "value"
}
```

**Erros:**
- `400 BAD_REQUEST` - ...
- `401 UNAUTHORIZED` - ...
- `403 FORBIDDEN` - ...
- `404 NOT_FOUND` - ...
- `409 CONFLICT` - ...
- `422 UNPROCESSABLE_ENTITY` - ...
- `429 TOO_MANY_REQUESTS` - ...
- `500 INTERNAL_SERVER_ERROR` - ...

## Cenarios de erro (detalhados)

### 1) <Erro>
**Status:** `422 UNPROCESSABLE_ENTITY`  
**Motivo:** ...  
**Exemplo de resposta (RFC 7807):**
```json
{
  "type": "https://blogames.com/errors/validation-error",
  "title": "Validation Error",
  "status": 422,
  "detail": "Campo X invalido",
  "instance": "/api/v1/<path>",
  "timestamp": "2026-05-21T10:30:00Z",
  "traceId": "abc123"
}
```

## Notas

- Paginacao: `page`, `size`, `sort` (se aplicavel)
- Campos opcionais e defaults
- Efeitos colaterais / eventos (se aplicavel)
